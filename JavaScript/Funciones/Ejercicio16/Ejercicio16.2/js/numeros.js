function numeros(pnumFin) {
    let numFin = pnumFin;
    let nums = '';
    for (let cont = 1; cont <= numFin; cont++) {
        nums += `${cont}\n`;
    }
    return nums;
}

const numerosExp = function (pnumFin) {
    let numFin = pnumFin;
    let nums = '';
    for (let cont = 1; cont <= numFin; cont++) {
        nums += `${cont}\n`;
    }
    return nums;
}
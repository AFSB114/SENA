function porcentajes(pnum) {
    let num = pnum;
    let porcentaje = num / 100;
    return porcentaje;
}

const porcentajesExp = function (pnum) {
    let num = pnum;
    let porcentaje = num / 100;
    return porcentaje;
}
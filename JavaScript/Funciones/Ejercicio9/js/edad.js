function edades(pedad) {
    let edad = pedad;
    if (edad >= 18) {
        return 'Mayor de edad';
    } else {
        return 'Menor de edad';
    }
}

const edadesExp = function (pedad) {
    let edad = pedad;
    if (edad >= 18) {
        return 'Mayor de edad';
    } else {
        return 'Menor de edad';
    }
}
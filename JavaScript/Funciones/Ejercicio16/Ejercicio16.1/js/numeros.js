function numeros(pnumFin) {
    let numFin = pnumFin;
    let cont = 0;
    let nums = '';
    while (cont < numFin) {
        cont += 1;
        nums += `${cont}\n`;
    }
    return nums;
}

const numerosExp = function (pnumFin) {
    let numFin = pnumFin;
    let cont = 0;
    let nums = '';
    while (cont < numFin) {
        cont += 1;
        nums += `${cont}\n`;
    }
    return nums;
}